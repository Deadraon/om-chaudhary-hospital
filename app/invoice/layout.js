// Standalone invoice print layout — NO dashboard sidebar, NO public header
// This is a completely clean page for printing
export default function InvoicePrintLayout({ children }) {
  return children;
}
